services:
  - type: web
    name: support-community-proxy
    env: node
    buildCommand: npm install && npm run build --prefix client || true
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production